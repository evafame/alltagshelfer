# Basis App-Konfiguration
APP_URL=http://localhost:3000
NODE_ENV=development

# Datenbank-Verbindung
DATABASE_URL=postgresql://user:pass@localhost:5432/alltagshelfer

# Stripe (für Zahlungen im Testmodus)
STRIPE_SECRET=sk_test_XXXXXXXXXXXXXXXX
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXX
PLATFORM_FEE_PERCENT=12

# WebSocket (Echtzeit-Nachrichten)
WS_URL=ws://localhost:3001

# Authentifizierung (z.B. JWT)
JWT_SECRET=replace_me

# Feature Flags (schalten bestimmte Features ein/aus)
FEATURE_SCREENING_REQUIRED=true
FEATURE_EMERGENCY_ENABLED=true
MIN_SCORE_HELPER_PAID=70
MIN_SCORE_HELPER_FREE=60
MIN_SCORE_SEEKER_PAID=60
MIN_SCORE_SEEKER_FREE=50
